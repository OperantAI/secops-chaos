experiments:
  - metadata:
      name: list-kubernetes-secrets
      type: list_kubernetes_secrets
      namespace: default
    parameters:
      executor_config:
        image: operant/executor-server
        target:
          target_port: 8080
          path: /experiment/listKubernetesSecrets/
        service_account_name: list-kubernetes-secrets
      namespaces:
        - default
        - kube-system
    